jui.ready([ "uix.window" ], function(win) {
    win_1 = win("#win_1", {
        width: 500,
        height: 300,
        left: "30%",
        top: 200,
        resize: true,
        move: true
    });
});